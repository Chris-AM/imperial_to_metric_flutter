
import 'package:flutter/material.dart';

class VolumeScreen extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return  Container(
      child: const Center(
        child: Text('Volume screen 2'),
      ),
    );
  }
}