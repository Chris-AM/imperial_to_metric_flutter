
import 'package:flutter/material.dart';

class SurfaceScreen extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return  Container(
      child: const Center(
        child: Text('Surface screen 2'),
      ),
    );
  }
}